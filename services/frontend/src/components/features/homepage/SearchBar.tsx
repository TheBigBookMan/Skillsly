

// TODO this will be a searchbar in middle of page to search up a skill to learn, how some sort of like sggestons when typing etc--- this could use server side rendering well etc

const SearchBar = () => {
    return (
        <div>
            <p>Search bar</p>
        </div>
    )
}

export default SearchBar;